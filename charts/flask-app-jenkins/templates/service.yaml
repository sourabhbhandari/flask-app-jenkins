apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-app-jenkins.fullname" . }}
  labels:
    {{- include "flask-app-jenkins.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-app-jenkins.selectorLabels" . | nindent 4 }}
